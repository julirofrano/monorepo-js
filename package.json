{
  "name": "monorepo-js",
  "private": true,
  "workspaces": {
    "packages": [
      "app1",
      "app2"
    ]
  },
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "webpack-dev-server": "^4.3.0"
  }
}
